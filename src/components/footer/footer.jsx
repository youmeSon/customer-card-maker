import React from 'react';
import styles from './footer.module.css';

const Footer = (props) => (
            <footer className={styles.footer}>
                <p className={styles.title}>A big business starts small</p>
            </footer>
    );

export default Footer;